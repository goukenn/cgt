Permet la capture d'un fichier en mode asynchrone.

<code class="igk-code-viewer phpcode">
$t->addAjxPickFile($this->getAppUri($fname."/uploadfile"), "{complete:igk.ajx.fn.replace_or_append_to_body}")
</code>