var b = igk.ctrl.get_schemas();
var table = $igk(document.createElement("table"));
for(var s in b){
	var tr = table.add("tr");
	table.appendChild(tr.owner);
	
	tr.add("td").setHtml(s);
	tr.add("td").setHtml(b[s].desc);
}
document.write(table.render());